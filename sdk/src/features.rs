//! Feature extraction utilities — re-exported from the signal processing core.
//! Placeholder that will re-export from the internal signal module.

pub struct FeatureExtractor;

impl FeatureExtractor {
    pub fn new(_sample_rate: u32) -> Self { Self }
}
